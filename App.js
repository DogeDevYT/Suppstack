import AppNavigator from './navigation/AppNavigator';

/**
 * The main entry point for the application.
 * It simply renders the main navigator.
 */
export default function App() {
  return <AppNavigator />;
}
